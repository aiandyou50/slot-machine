{
  "version": 1,
  "routes": [
    { "src": "/api/(.*)", "dest": "/functions/$1" },
    { "src": "/functions/(.*)", "dest": "/functions/$1" },
    { "src": "/(.*)", "dest": "/$1" }
  ],
  "overrides": {
    "404": "/index.html"
  }
}